# ── Gravs — Requirements ─────────────────────────────────────────────────────
# Em produção (PythonAnywhere): pip install -r requirements.txt --user

# Framework principal
Flask>=3.0,<4.0

# Autenticação
Flask-Login>=0.6,<1.0
Werkzeug>=3.0,<4.0

# Segurança
Flask-WTF>=1.2,<2.0
Flask-Limiter>=3.5,<4.0

# Performance — compressão gzip automática
Flask-Compress>=1.14,<2.0

# Feriados brasileiros
holidays>=0.46,<1.0

# Agendador de tarefas (resumo mensal por email)
APScheduler>=3.10

# Exportação Excel
openpyxl>=3.1,<4.0

# Testes
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
